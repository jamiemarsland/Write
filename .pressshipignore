# WordPress.org listing assets (icon, banner, screenshots) — synced to the SVN
# assets/ directory on release, never shipped inside the installable plugin zip.
.wordpress-org
.wordpress-org/**

# Don't ship the ignore file itself.
.pressshipignore
